name: 🧪 Análisis Estático - Calidad y Seguridad del Código

on:
  push:
    branches: [main]
  pull_request:

jobs:
  analisis:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout del repositorio
      - name: Checkout del repositorio
        uses: actions/checkout@v4

      # 2️⃣ Instalar Flutter (VERSION COMPATIBLE CON DART 3.8.1)
      - name: Instalar Flutter 3.32.4 (Compatible con Dart 3.8.1)
        uses: subosito/flutter-action@v2
        with:
          # Usamos la versión 3.32.4 según lo confirmado
          flutter-version: '3.32.4' 
          channel: 'stable'
          cache: true

      - name: Verificar versión de Flutter
        run: flutter --version

      # 3️⃣ Obtener dependencias (EJECUTADO DENTRO DE LA CARPETA DE LA APLICACIÓN)
      - name: Obtener dependencias
        run: cd SosMascota && flutter pub get

      # 4️⃣ Ejecutar pruebas con cobertura (EJECUTADO DENTRO DE LA CARPETA DE LA APLICACIÓN)
      - name: Ejecutar pruebas con cobertura
        run: cd SosMascota && flutter test --coverage

      # 5️⃣ Análisis con SonarCloud 
      - name: Análisis con SonarCloud
        uses: SonarSource/sonarcloud-github-action@v2
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        with:
          # APUNTA A LA CARPETA DE LA APP 
          projectBaseDir: SosMascota 
          args: >
            -Dsonar.projectKey=${{ secrets.SONAR_PROJECT_KEY }}
            -Dsonar.organization=${{ secrets.SONAR_ORG }}
            -Dsonar.qualitygate.wait=true
            # RUTA DEL REPORTE ES RELATIVA A 'SosMascota'
            -Dsonar.dart.coverage.reportPaths=coverage/lcov.info
            -Dsonar.c.file.suffixes=-
            -Dsonar.cpp.file.suffixes=-
            -Dsonar.objc.file.suffixes=-

      # 6️⃣ Análisis con Semgrep
      - name: Análisis con Semgrep
        uses: returntocorp/semgrep-action@v1
        with:
          config: "p/ci"

      # 7️⃣ Análisis con Snyk
      - name: Análisis con Snyk
        uses: snyk/actions/setup@v1
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
        with:
          command: test
