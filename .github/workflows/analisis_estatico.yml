name: 🧪 Análisis Estático - Calidad y Seguridad del Código

on:
  push:
    branches: [main]
  pull_request:

jobs:
  analisis:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout del repositorio
      - name: Checkout del repositorio
        uses: actions/checkout@v4

      # 2️⃣ Instalar Flutter
      - name: Instalar Flutter 3.32.4 (Compatible con Dart 3.8.1)
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.32.4'
          channel: 'stable'
          cache: true

      - name: Verificar versión de Flutter
        run: flutter --version

      # 3️⃣ Instalar Dart CLI (necesario para SonarCloud)
      - name: Instalar Dart SDK
        uses: dart-lang/setup-dart@v1
        with:
          sdk: stable

      - name: Verificar versión de Dart
        run: dart --version

      # 4️⃣ Obtener dependencias
      - name: Obtener dependencias
        run: flutter pub get

      # 5️⃣ Ejecutar pruebas con cobertura
      - name: Ejecutar pruebas con cobertura
        run: flutter test --coverage

      # 6️⃣ Convertir LCOV a XML
      - name: Convertir cobertura LCOV a XML
        run: |
          dart pub global activate coverage
          dart pub global run coverage:format_coverage \
            --lcov \
            --in coverage/lcov.info \
            --out coverage/coverage.xml \
            --report-on=lib

      # 7️⃣ Análisis con SonarCloud
      - name: Análisis con SonarCloud
        uses: SonarSource/sonarcloud-github-action@v2
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        with:
          projectBaseDir: .
          args: >
            -Dsonar.projectKey=${{ secrets.SONAR_PROJECT_KEY }}
            -Dsonar.organization=${{ secrets.SONAR_ORG }}
            -Dsonar.qualitygate.wait=true
            -Dsonar.javascript.lcov.reportPaths=coverage/lcov.info
            -Dsonar.dart.analyzerPath="/usr/bin/dart"
            -Dsonar.c.file.suffixes=-
            -Dsonar.cpp.file.suffixes=-
            -Dsonar.objc.file.suffixes=-

      # 8️⃣ Análisis con Semgrep
      - name: Análisis con Semgrep
        uses: returntocorp/semgrep-action@v1
        with:
          config: "p/ci"

      # 9️⃣ Análisis con Snyk
      - name: Análisis con Snyk
        uses: snyk/actions/setup@v1
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
        with:
          command: test
